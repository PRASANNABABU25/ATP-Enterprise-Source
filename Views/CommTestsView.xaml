<UserControl x:Class="atp_enterprise_app_wpf.Views.CommTestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="UUT Communication Ports &amp; Audio Channels" 
                   Foreground="{StaticResource TextPrimaryBrush}" 
                   FontSize="22" 
                   FontFamily="{StaticResource TitleFont}" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,15" />

        <UniformGrid Grid.Row="1" Columns="2">
            <!-- Serial COM Ports Card -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="SERIAL PORT REGISTERS (RS-232 / RS-422)" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <ListBox Grid.Row="1" x:Name="LstComPorts" Background="#0b0e14" BorderThickness="1" BorderBrush="{StaticResource BorderCardBrush}" Foreground="#FFFFFF" Margin="0,5,0,10" Padding="8"/>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Queries local COM hardware registry keys." Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="Scan Ports" Style="{StaticResource FluentButton}" Click="ScanPorts_Click"/>
                    </Grid>
                </Grid>
            </Border>

            <!-- Audio VU Level Card -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="10,0,0,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="CHASSIS MICROPHONE GAIN VERIFICATION" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,12"/>
                    <TextBlock Text="Reads live decibel amplitude voltage rails from standard Windows sound audio devices." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,20"/>
                    
                    <TextBlock Text="Microphone Level VU:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                    <TextBlock Text="Device Not Connected" x:Name="TxtMicLevel" Foreground="{StaticResource StatusWarnBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,20"/>

                    <TextBlock Text="Standard Headset Jack:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                    <TextBlock Text="Not Connected" Foreground="{StaticResource TextMutedBrush}" FontSize="14" FontWeight="SemiBold" Margin="0,2,0,10"/>
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
