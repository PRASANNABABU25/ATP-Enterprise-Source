<UserControl x:Class="atp_enterprise_app_wpf.Views.NetworkPerformanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080"
             Background="#1e1e1e" Foreground="#d4d4d4">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Top Bar -->
        <Grid Grid.Row="0" Background="#181818">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="16,8" Grid.Column="0">
                <Path Data="M22,12 L18,12 15,21 9,3 6,12 2,12" Stroke="#d7ba7d" StrokeThickness="2" Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="NetPerf - A Simple Network Performance Testing Tool for Windows" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" Grid.Column="2" Margin="16,8" VerticalAlignment="Center">
                <Button Content="!" Background="#007acc" Foreground="White" BorderThickness="0" Padding="8,2" Margin="0,0,8,0" FontWeight="Bold" FontSize="11" Width="24"/>
                <Button Content="Donate" Background="#c53030" Foreground="White" BorderThickness="0" Padding="8,2" Margin="0,0,16,0" FontWeight="Bold" FontSize="11"/>
                <TextBlock Text="—" Foreground="#d4d4d4" Margin="8,0" VerticalAlignment="Center"/>
                <TextBlock Text="☐" Foreground="#d4d4d4" Margin="8,0" VerticalAlignment="Center" FontSize="16"/>
                <TextBlock Text="✕" Foreground="#d4d4d4" Margin="8,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Main Layout -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <ScrollViewer Grid.Column="0" Background="#252526" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="16">
                    
                    <!-- Mode -->
                    <Border Background="#2d2d30" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Mode" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
                            <RadioButton x:Name="RbClient" Content="Client" IsChecked="True" Foreground="#d4d4d4" Margin="0,0,0,8"/>
                            <RadioButton x:Name="RbServer" Content="Server" Foreground="#d4d4d4"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Connection -->
                    <Border Background="#2d2d30" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Connection" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
                            <TextBlock Text="Server Address" Foreground="#a0a0a0" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="TxtHost" Text="127.0.0.1" Background="#1e1e1e" Foreground="#d4d4d4" BorderBrush="#3e3e42" Padding="6" Margin="0,0,0,12"/>
                            
                            <TextBlock Text="Port" Foreground="#a0a0a0" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="TxtPort" Text="5201" Background="#1e1e1e" Foreground="#d4d4d4" BorderBrush="#3e3e42" Padding="6"/>
                        </StackPanel>
                    </Border>

                    <!-- Test Configuration -->
                    <Border Background="#2d2d30" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Test Configuration" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
                            
                            <TextBlock Text="Protocol" Foreground="#a0a0a0" FontSize="12" Margin="0,0,0,4"/>
                            <ComboBox x:Name="CmbProtocol" Background="#1e1e1e" Foreground="#d4d4d4" BorderBrush="#3e3e42" Padding="6" Margin="0,0,0,12" SelectedIndex="0">
                                <ComboBoxItem Content="TCP"/>
                                <ComboBoxItem Content="UDP"/>
                            </ComboBox>
                            
                            <TextBlock Text="Direction" Foreground="#a0a0a0" FontSize="12" Margin="0,0,0,4"/>
                            <ComboBox x:Name="CmbDirection" Background="#1e1e1e" Foreground="#d4d4d4" BorderBrush="#3e3e42" Padding="6" Margin="0,0,0,12" SelectedIndex="0">
                                <ComboBoxItem Content="Upload"/>
                                <ComboBoxItem Content="Download"/>
                            </ComboBox>
                            
                            <TextBlock Text="Duration (seconds)" Foreground="#a0a0a0" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="TxtDuration" Text="10" Background="#1e1e1e" Foreground="#d4d4d4" BorderBrush="#3e3e42" Padding="6" Margin="0,0,0,12"/>
                            
                            <TextBlock Text="Parallel Streams" Foreground="#a0a0a0" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="TxtStreams" Text="1" Background="#1e1e1e" Foreground="#d4d4d4" BorderBrush="#3e3e42" Padding="6" Margin="0,0,0,12"/>
                            
                            <TextBlock Text="Buffer Size (KB)" Foreground="#a0a0a0" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="TxtBufferSize" Text="128" Background="#1e1e1e" Foreground="#d4d4d4" BorderBrush="#3e3e42" Padding="6"/>
                        </StackPanel>
                    </Border>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="BtnStart" Content="Start Test" Grid.Column="0" Background="#d7ba7d" Foreground="#1e1e1e" FontWeight="SemiBold" Padding="0,10" Margin="0,0,4,0" BorderThickness="0" Click="BtnStart_Click"/>
                        <Button x:Name="BtnStop" Content="Stop Test" Grid.Column="1" Background="#3c3c3c" Foreground="#d4d4d4" FontWeight="SemiBold" Padding="0,10" Margin="4,0,0,0" BorderThickness="0" Click="BtnStop_Click" IsEnabled="False"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
            
            <!-- Content Area -->
            <Grid Grid.Column="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TxtStatus" Text="Ready" Foreground="#d7ba7d" FontWeight="Bold" FontSize="14" Grid.Row="0" Margin="0,0,0,16"/>
                
                <!-- Metrics -->
                <Grid Grid.Row="1" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6" Padding="16" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="Current" Foreground="#a0a0a0" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="TxtCurrent" Text="0.00" FontWeight="Bold" FontSize="20"/>
                                <TextBlock Text="Mbps" Foreground="#a0a0a0" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6" Padding="16" Margin="8,0,8,0">
                        <StackPanel>
                            <TextBlock Text="Average" Foreground="#a0a0a0" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="TxtAverage" Text="0.00" FontWeight="Bold" FontSize="20"/>
                                <TextBlock Text="Mbps" Foreground="#a0a0a0" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6" Padding="16" Margin="8,0,8,0">
                        <StackPanel>
                            <TextBlock Text="Peak" Foreground="#a0a0a0" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock x:Name="TxtPeak" Text="0.00" FontWeight="Bold" FontSize="20"/>
                                <TextBlock Text="Mbps" Foreground="#a0a0a0" FontSize="14" VerticalAlignment="Bottom" Margin="4,0,0,2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="3" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6" Padding="16" Margin="8,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Transferred" Foreground="#a0a0a0" FontSize="12"/>
                            <TextBlock x:Name="TxtTransferred" Text="0.00 B" FontWeight="Bold" FontSize="18" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Chart -->
                <Border Grid.Row="2" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6" Margin="0,0,0,16">
                    <lvc:CartesianChart x:Name="ChartThroughput" DisableAnimations="True" Hoverable="False" DataTooltip="{x:Null}">
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Throughput (Mbps)" MinValue="0" Foreground="#a0a0a0">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Stroke="#333333"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Time (seconds)" Foreground="#a0a0a0">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Stroke="Transparent"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.Series>
                            <lvc:LineSeries x:Name="SeriesThroughput" Values="{Binding ChartValues}" Stroke="#d7ba7d" Fill="Transparent" PointGeometry="{x:Null}" LineSmoothness="0"/>
                        </lvc:CartesianChart.Series>
                    </lvc:CartesianChart>
                </Border>

                <!-- Network Metrics Header -->
                <Border Grid.Row="3" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1" CornerRadius="6,6,0,0" Padding="16,12" Margin="0,0,0,0">
                    <TextBlock Text="Network Metrics" FontWeight="SemiBold" FontSize="14"/>
                </Border>

                <!-- Status Log Header and Area -->
                <Border Grid.Row="4" Background="#252526" BorderBrush="#3e3e42" BorderThickness="1,0,1,1" CornerRadius="0,0,6,6" Margin="0,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" BorderBrush="#3e3e42" BorderThickness="0,1,0,1" Padding="16,12">
                            <TextBlock Text="Status Log" FontWeight="SemiBold" FontSize="14"/>
                        </Border>
                        
                        <ListBox x:Name="ListLogs" Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="#cccccc" FontFamily="Consolas" FontSize="12" Margin="8"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
