<UserControl x:Class="atp_enterprise_app_wpf.Views.FunctionalTestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="UUT Functional Diagnostics" 
                   Foreground="{StaticResource TextPrimaryBrush}" 
                   FontSize="22" 
                   FontFamily="{StaticResource TitleFont}" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,15" />

        <UniformGrid Grid.Row="1" Columns="2" Rows="2">
            <!-- System Boot Verification -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,10,10">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="UUT BOOT &amp; UPTIME MONITOR" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Validates system BIOS load time and kernel initialization uptime." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Measured System Uptime:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                            <TextBlock x:Name="TxtUptime" Text="Calculating..." Foreground="#FFFFFF" FontSize="16" FontWeight="SemiBold" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Border Grid.Column="1" Background="{StaticResource StatusPassBrush}" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                            <TextBlock Text="PASS" Foreground="#FFFFFF" FontSize="11" FontWeight="Bold"/>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Real-Time Clock Drift -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="10,0,0,10">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="REAL-TIME CLOCK SYNC (RTC)" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Measures system hardware clock skew against precise high-resolution query performance counters." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="RTC Clock Drift:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                            <TextBlock x:Name="TxtRtcDrift" Text="0.000 ms" Foreground="#FFFFFF" FontSize="16" FontWeight="SemiBold" Margin="0,2,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="Verify Drift" Style="{StaticResource FluentButtonSecondary}" Click="VerifyRtc_Click" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- CapsLock Keyboard Flash -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,10,10,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="KEYBOARD INDICATORS DIAGNOSTIC" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Sends low-level physical virtual keyboard events to blink UUT Caps Lock LEDs, verifying logic controller lines." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,15"/>
                    <Button Content="Flash Caps Lock LED" Style="{StaticResource FluentButton}" Click="FlashLed_Click" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <!-- Sound Buzzer Synth -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="10,10,0,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="CHASSIS BUZZER SYNTHESIS" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Fires hardware motherboards beepers or audio systems with a 2000 Hz synthesis tone to test acoustic alarms." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" FontSize="11" Margin="0,0,0,15"/>
                    <Button Content="Emit Audio Beep" Style="{StaticResource FluentButton}" Click="EmitBeep_Click" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>
