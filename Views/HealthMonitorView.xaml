<UserControl x:Class="atp_enterprise_app_wpf.Views.HealthMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="850">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <!-- Title -->
            <RowDefinition Height="Auto" />
            <!-- Controls & Engine Status -->
            <RowDefinition Height="Auto" />
            <!-- Mini Charts -->
            <RowDefinition Height="Auto" />
            <!-- Sensor registries and parameters -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="System Health &amp; Dynamic Telemetry" 
                   Foreground="{StaticResource TextPrimaryBrush}" 
                   FontSize="22" 
                   FontFamily="{StaticResource TitleFont}" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,10" />

        <!-- Row 1: Engine Controls & Live Status -->
        <Border Grid.Row="1" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnStart" Content="Start Engine" Style="{StaticResource FluentButton}" Click="StartMonitoring_Click" Width="110" Margin="0,0,10,0"/>
                    <Button x:Name="BtnStop" Content="Stop Engine" Style="{StaticResource FluentButtonSecondary}" Click="StopMonitoring_Click" Width="110"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="25,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Engine Status: " Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center"/>
                    <Border x:Name="StatusIndicator" Background="#237804" CornerRadius="4" Padding="8,3" VerticalAlignment="Center" Margin="5,0,0,0">
                        <TextBlock x:Name="TxtStatus" Text="ACTIVE" Foreground="#FFFFFF" FontSize="10" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <TextBlock Grid.Column="3" x:Name="TxtLastUpdate" Text="Last Scan: Awaiting Run" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Row 2: Mini Canvas Charts (CPU, RAM, Temp) -->
        <UniformGrid Grid.Row="2" Columns="3" Rows="1" Margin="0,0,0,15">
            <!-- CPU Temp Hist -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="CPU TEMPERATURE HISTORY" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9.5" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtLiveTemp" Text="N/A" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Border Height="60" Background="#0b0e14" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1" CornerRadius="4">
                        <Canvas x:Name="CanvasTemp" ClipToBounds="True"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- CPU Workload Hist -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="8,0,8,0">
                <StackPanel>
                    <TextBlock Text="CPU CORE UTILIZATION" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9.5" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtLiveCpu" Text="0.0 %" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Border Height="60" Background="#0b0e14" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1" CornerRadius="4">
                        <Canvas x:Name="CanvasCpu" ClipToBounds="True"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Memory Hist -->
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="RAM PAGES UTILIZATION" Foreground="{StaticResource TextSecondaryBrush}" FontSize="9.5" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtLiveRam" Text="0.0 %" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                    <Border Height="60" Background="#0b0e14" BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="1" CornerRadius="4">
                        <Canvas x:Name="CanvasRam" ClipToBounds="True"/>
                    </Border>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Row 3: Detail Sections and Sensor Registry -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*"/>
                <ColumnDefinition Width="2.2*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 0: Numeric parameters lists -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,12,0">
                <StackPanel>
                    <!-- Processor details -->
                    <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="PROCESSOR METRICS" Foreground="{StaticResource AccentBlueBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Core Load:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="TxtDtlCpuLoad" Text="0.0%" Foreground="#FFFFFF" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Current Operating Frequency:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="TxtDtlCpuFreq" Text="Calculating..." Foreground="#FFFFFF" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Thermal Throttling Interrupts:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="TxtDtlCpuThrottling" Text="Inactive" Foreground="#FFFFFF" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Memory Details -->
                    <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="MEMORY METRICS" Foreground="{StaticResource AccentBlueBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Total Allocated Pages:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="TxtDtlRamTotal" Text="0.0 GB" Foreground="#FFFFFF" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Free Available Pages:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="TxtDtlRamFree" Text="0.0 GB" Foreground="#FFFFFF" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Pages Allocation Rate:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="TxtDtlRamPct" Text="0.0%" Foreground="#FFFFFF" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Power Details -->
                    <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                        <StackPanel>
                            <TextBlock Text="DC POWER AND BATTERY DIAGNOSTIC" Foreground="{StaticResource AccentBlueBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Line Power Status:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock x:Name="TxtDtlPowerSource" Text="Online (AC)" Foreground="#FFFFFF" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="DC Rails (12V/5V/3.3V):" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="Sensor Not Supported" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Chassis Fans Coolant Speed:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                <TextBlock Text="Sensor Not Supported" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Column 1: Centralized Sensor Registry Table -->
            <Border Grid.Column="1" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="AUDIT SENSOR REGISTRY PROFILE" Foreground="{StaticResource AccentBlueBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,12"/>
                    
                    <DataGrid Grid.Row="1" x:Name="GridSensorRegistry" Style="{StaticResource FluentDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Sensor Name" Binding="{Binding Name}" Width="1.8*"/>
                            <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                            <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="0.6*"/>
                            
                            <!-- Health status badge template -->
                            <DataGridTemplateColumn Header="Health State" Width="1.1*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="3" Padding="6,2" x:Name="Badge">
                                            <TextBlock Text="{Binding HealthState}" Foreground="#FFFFFF" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        </Border>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding HealthState}" Value="Normal">
                                                <Setter TargetName="Badge" Property="Background" Value="{StaticResource StatusPassBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding HealthState}" Value="Warning">
                                                <Setter TargetName="Badge" Property="Background" Value="{StaticResource StatusWarnBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding HealthState}" Value="Critical">
                                                <Setter TargetName="Badge" Property="Background" Value="{StaticResource StatusFailBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding HealthState}" Value="Unavailable">
                                                <Setter TargetName="Badge" Property="Background" Value="#3a3e4d"/>
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
