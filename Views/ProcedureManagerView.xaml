<UserControl x:Class="atp_enterprise_app_wpf.Views.ProcedureManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             Background="#1E1E1E">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ATP PROCEDURE MANAGER" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Procedures List -->
            <Border Background="#2D2D30" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Procedure Library" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    
                    <ListBox x:Name="LstProcedures" Grid.Row="1" Background="Transparent" Foreground="White" BorderThickness="0" SelectionChanged="LstProcedures_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,5">
                                    <TextBlock Text="{Binding ProcedureTitle}" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{Binding ProductFamily}" Foreground="#AAAAAA" FontSize="12"/>
                                    <TextBlock Text="{Binding Revision, StringFormat='Rev: {0}'}" Foreground="#007ACC" FontSize="12"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button x:Name="BtnNew" Content="New" Width="70" Height="30" Background="#007ACC" Foreground="White" Margin="0,0,10,0" Click="BtnNew_Click"/>
                        <Button x:Name="BtnClone" Content="Clone" Width="70" Height="30" Background="#3E3E42" Foreground="White" Click="BtnClone_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Procedure Editor -->
            <Border Grid.Column="2" Background="#2D2D30" CornerRadius="8" Padding="20">
                <Grid x:Name="EditorPanel" IsEnabled="False">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Procedure Details" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Procedure Title:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtTitle" Background="#3E3E42" Foreground="White" Padding="5" Margin="0,0,0,10"/>

                            <TextBlock Text="Product Family:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtFamily" Background="#3E3E42" Foreground="White" Padding="5" Margin="0,0,0,10"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Revision:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtRevision" Background="#3E3E42" Foreground="White" Padding="5" Margin="0,0,0,10"/>

                            <TextBlock Text="Author:" Foreground="#CCCCCC" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtAuthor" Background="#3E3E42" Foreground="White" Padding="5" Margin="0,0,0,10"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Grid.Row="2" Text="Test Sequence Configuration" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,15,0,10"/>

                    <DataGrid x:Name="GridTestSequence" Grid.Row="3" Background="#1E1E1E" Foreground="White" BorderThickness="0"
                              AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled}"/>
                            <DataGridCheckBoxColumn Header="Mandatory" Binding="{Binding IsMandatory}"/>
                            <DataGridTextColumn Header="Test ID" Binding="{Binding TestId}" IsReadOnly="True" Foreground="#CCCCCC"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Limit Num" Binding="{Binding MinimumNumericValue}"/>
                            <DataGridTextColumn Header="Timeout (ms)" Binding="{Binding TimeoutMs}"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button x:Name="BtnSave" Content="Save Procedure" Width="120" Height="35" Background="#007ACC" Foreground="White" FontWeight="Bold" Click="BtnSave_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
