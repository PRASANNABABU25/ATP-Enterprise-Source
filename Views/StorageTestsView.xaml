<UserControl x:Class="atp_enterprise_app_wpf.Views.StorageTestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="700">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="UUT Storage Performance Benchmarks" 
                   Foreground="{StaticResource TextPrimaryBrush}" 
                   FontSize="22" 
                   FontFamily="{StaticResource TitleFont}" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,15" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Test Configuration Card -->
            <Border Grid.Row="0" Background="{StaticResource BgCardBrush}" 
                    BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" 
                    CornerRadius="8" Padding="16" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="BENCHMARK PARAMETERS" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="Write/read block buffer size: 20MB sequential stream." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtTargetPath" Text="Target directory: C:\Temp" Foreground="#FFFFFF" FontSize="13" FontWeight="SemiBold"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="Run Benchmark" Style="{StaticResource FluentButton}" Click="RunBenchmark_Click" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Diagnostic Speed Bars Card -->
            <Border Grid.Row="1" Background="{StaticResource BgCardBrush}" 
                    BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" 
                    CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="PERFORMANCE DIAGNOSIS GRAPH" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,20"/>

                    <!-- Speeds Bars Grid -->
                    <StackPanel Grid.Row="1" VerticalAlignment="Center">
                        <!-- Write Speed -->
                        <TextBlock Text="SEQUENTIAL WRITE RATE" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                        <Grid Margin="0,0,0,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="#0b0e14" Height="22" CornerRadius="3" Padding="2">
                                <Border x:Name="WriteBar" Background="{StaticResource AccentBlueBrush}" CornerRadius="2" HorizontalAlignment="Left" Width="0"/>
                            </Border>
                            <TextBlock Grid.Column="1" x:Name="TxtWriteSpeed" Text="0.00 MB/s" Foreground="#FFFFFF" FontSize="13" FontWeight="Bold" Width="95" Margin="10,0,0,0" VerticalAlignment="Center" TextAlignment="Right"/>
                        </Grid>

                        <!-- Read Speed -->
                        <TextBlock Text="SEQUENTIAL READ RATE" Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="#0b0e14" Height="22" CornerRadius="3" Padding="2">
                                <Border x:Name="ReadBar" Background="#52c41a" CornerRadius="2" HorizontalAlignment="Left" Width="0"/>
                            </Border>
                            <TextBlock Grid.Column="1" x:Name="TxtReadSpeed" Text="0.00 MB/s" Foreground="#FFFFFF" FontSize="13" FontWeight="Bold" Width="95" Margin="10,0,0,0" VerticalAlignment="Center" TextAlignment="Right"/>
                        </Grid>
                    </StackPanel>

                    <!-- Integrity Status -->
                    <Border Grid.Row="2" Background="#141d26" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,15,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Block Adler-32 Checksum Validation Integrity Check:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" x:Name="TxtIntegrity" Text="Awaiting Test Run" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
