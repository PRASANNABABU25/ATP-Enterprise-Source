<UserControl x:Class="atp_enterprise_app_wpf.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             Loaded="UserControl_Loaded">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Left: Sessions -->
        <Border Grid.Column="0" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                
                <TextBlock Text="HISTORICAL SESSIONS" Foreground="{StaticResource AccentBlueBrush}" FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                
                <TextBox x:Name="TxtSearchSession" Grid.Row="1" Style="{DynamicResource DarkTextBox}" Margin="0,0,0,10" TextChanged="TxtSearchSession_TextChanged" />
                
                <ListBox x:Name="LstSessions" Grid.Row="2" Background="Transparent" BorderThickness="0" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         SelectionChanged="LstSessions_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1" Padding="5,10" Background="Transparent">
                                <StackPanel>
                                    <TextBlock Text="{Binding UnitSerialNumber}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding StartTime, StringFormat=g}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,2,0,0"/>
                                    <TextBlock Text="{Binding OverallResult}" Foreground="{Binding OverallResult, Converter={StaticResource StatusToColorConverter}}" FontSize="11" FontWeight="Bold" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,5" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4">
                                            <ContentPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#1a2533" />
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#121822" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <!-- Right: Report Generation -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Top Action Bar -->
            <Border Grid.Row="0" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Report Type:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox x:Name="CboReportType" Width="200" SelectedIndex="0">
                            <ComboBoxItem Content="Full ATP Compliance Certificate" />
                            <!-- <ComboBoxItem Content="Hardware Configuration Report" /> -->
                            <!-- <ComboBoxItem Content="Audit Trail Report" /> -->
                        </ComboBox>
                    </StackPanel>

                    <Button Grid.Column="2" Content="Generate Preview" Style="{StaticResource FluentButton}" Click="GeneratePreview_Click" />
                </Grid>
            </Border>

            <!-- Preview Area -->
            <Border Grid.Row="1" Background="#eef1f5" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,15">
                <FlowDocumentScrollViewer x:Name="PreviewViewer" Document="{Binding CurrentPreviewDocument}" Zoom="80" IsToolBarVisible="True" Margin="2" />
            </Border>

            <!-- Export Actions -->
            <Border Grid.Row="2" Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="The exported document reflects the exact session data without re-running any tests." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" VerticalAlignment="Center" TextWrapping="Wrap" Margin="0,0,20,0"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Export to PDF" Style="{StaticResource FluentButton}" Click="ExportPdf_Click" Margin="0,0,10,0"/>
                        <Button Content="Export to Word (DOCX)" Style="{StaticResource FluentButtonSecondary}" Click="ExportDocx_Click" Margin="0,0,10,0"/>
                        <Button Content="Export to Excel (XLSX)" Style="{StaticResource FluentButtonSecondary}" Click="ExportXlsx_Click" Margin="0,0,10,0"/>
                        <Button Content="Export CSV" Style="{StaticResource FluentButtonSecondary}" Click="ExportCsv_Click" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
