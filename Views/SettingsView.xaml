<UserControl x:Class="atp_enterprise_app_wpf.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="700">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="ATP Threshold Bounds Configuration" 
                   Foreground="{StaticResource TextPrimaryBrush}" 
                   FontSize="22" 
                   FontFamily="{StaticResource TitleFont}" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,15" />

        <!-- Limits Card -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderCardBrush}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="PARAMETRIC PASS/FAIL SPECIFICATION LIMITS" Foreground="{StaticResource AccentBlueBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Temp Limit -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,15" VerticalAlignment="Center">
                            <TextBlock Text="Max Thermal Temperature Threshold (°C)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="UUT fails sequence if any sensor exceeds this temperature." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TxtTempMax" Style="{DynamicResource DarkTextBox}" Text="75" Margin="0,0,0,15" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>

                        <!-- Disk Read Limit -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,15" VerticalAlignment="Center">
                            <TextBlock Text="Min Sequential Storage Read Rate (MB/s)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="Specifies the minimum acceptable disk read bandwidth." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="1" Grid.Column="1" x:Name="TxtReadMin" Style="{DynamicResource DarkTextBox}" Text="150" Margin="0,0,0,15" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>

                        <!-- Disk Write Limit -->
                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,15" VerticalAlignment="Center">
                            <TextBlock Text="Min Sequential Storage Write Rate (MB/s)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="Specifies the minimum acceptable disk write bandwidth." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="2" Grid.Column="1" x:Name="TxtWriteMin" Style="{DynamicResource DarkTextBox}" Text="80" Margin="0,0,0,15" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>

                        <!-- CPU Warn Limit -->
                        <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,15" VerticalAlignment="Center">
                            <TextBlock Text="CPU Utilization Warning Level (%)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="Triggers a health Warning if CPU workload goes above this value." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="3" Grid.Column="1" x:Name="TxtCpuWarn" Style="{DynamicResource DarkTextBox}" Text="80" Margin="0,0,0,15" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>

                        <!-- CPU Critical Limit -->
                        <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,0,15" VerticalAlignment="Center">
                            <TextBlock Text="CPU Utilization Critical Level (%)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="Triggers a Critical fault state if CPU workload reaches this value." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="4" Grid.Column="1" x:Name="TxtCpuCritical" Style="{DynamicResource DarkTextBox}" Text="95" Margin="0,0,0,15" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>

                        <!-- RAM Warn Limit -->
                        <StackPanel Grid.Row="5" Grid.Column="0" Margin="0,0,0,15" VerticalAlignment="Center">
                            <TextBlock Text="RAM Utilization Warning Level (%)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="Triggers a health Warning if Memory allocation exceeds this value." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="5" Grid.Column="1" x:Name="TxtRamWarn" Style="{DynamicResource DarkTextBox}" Text="85" Margin="0,0,0,15" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>

                        <!-- RAM Critical Limit -->
                        <StackPanel Grid.Row="6" Grid.Column="0" Margin="0,0,0,15" VerticalAlignment="Center">
                            <TextBlock Text="RAM Utilization Critical Level (%)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="Triggers a Critical fault state if memory allocation reaches this value." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="6" Grid.Column="1" x:Name="TxtRamCritical" Style="{DynamicResource DarkTextBox}" Text="95" Margin="0,0,0,15" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>

                        <!-- Monitoring Sample Interval -->
                        <StackPanel Grid.Row="7" Grid.Column="0" Margin="0,0,0,15" VerticalAlignment="Center">
                            <TextBlock Text="Live Telemetry Sampling Rate (ms)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="Specifies the refresh polling interval rate for real-time monitoring sensors." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="7" Grid.Column="1" x:Name="TxtRefreshInterval" Style="{DynamicResource DarkTextBox}" Text="1000" Margin="0,0,0,15" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>

                        <!-- Rolling History Buffer Size -->
                        <StackPanel Grid.Row="8" Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Rolling History Buffer Size (samples)" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="SemiBold"/>
                            <TextBlock Text="Specifies the number of historical measurements kept in active memory charts." Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                        </StackPanel>
                        <TextBox Grid.Row="8" Grid.Column="1" x:Name="TxtHistorySize" Style="{DynamicResource DarkTextBox}" Text="60" VerticalAlignment="Center" Width="100" HorizontalAlignment="Right"/>
                    </Grid>

                    <Separator BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0.5" Margin="0,20,0,15"/>

                    <Button Content="Save Threshold Parameters" Style="{StaticResource FluentButton}" Click="SaveLimits_Click" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>

    <UserControl.Resources>
        <!-- Dark TextBox Style Copy for SettingsView -->
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0b0e14" />
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderCardBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="CaretBrush" Value="#FFFFFF" />
            <Setter Property="FontFamily" Value="{StaticResource MainFont}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlueBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
</UserControl>
